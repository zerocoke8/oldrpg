# 프로젝트: LLM 텍스트 머드

웹 브라우저에서 돌아가는 멀티플레이어 텍스트 머드. 세계의 묘사·NPC 대사·퀘스트를
LLM이 생성하지만, 게임 규칙과 상태는 전부 결정론적 코드가 소유한다.

---

## 절대 규칙

이 네 가지는 성능·편의를 이유로도 어기지 않는다. 어기는 편이 빨라 보이면,
코드를 고치기 전에 사람에게 먼저 물어볼 것.

**1. 엔진이 진실, LLM은 묘사만 한다.**
맵 구조, HP, 데미지, 인벤토리, 좌표, 퀘스트 진행도는 전부 일반 코드가 계산한다.
LLM에는 *이미 확정된 결과*를 넘겨서 문장으로 옮기게만 한다.
LLM 출력이 게임 상태를 바꾸는 경로는 존재해서는 안 된다.

**2. 생성은 서버에서, 딱 한 번.**
클라이언트는 절대 LLM을 호출하지 않는다. API 키는 서버에만 있다.
생성된 텍스트는 즉시 DB에 기록되고, 그 시점부터 모든 플레이어에게 동일하다.
같은 방에 두 명이 동시 진입할 수 있으므로 좌표 단위 생성 락이 필요하다.

**3. 씨앗은 불변, 묘사는 (씨앗 + 플래그)의 함수다.**
이벤트가 세계를 바꿀 때 기존 텍스트를 고쳐 쓰지 않는다. 원본 씨앗과 새 플래그로
*다시 렌더링*한다. 기존 텍스트를 LLM에 먹여 수정시키면 몇 번 만에 원형이 사라진다.

**4. 플레이어를 LLM 앞에 세워두지 않는다.**
이벤트 직후 보여줄 문장은 미리 작성된 결정론적 텍스트를 쓴다.
재생성은 백그라운드 큐로 돌리고, 완료되면 조용히 교체한다.

---

## 데이터 모델

```
rooms          id, x, y, seed, region, sensitive_flags[]   -- 절대 변경 안 됨
world_flags    key, value                                  -- 엔진 소유
room_text      room_id, state_hash, text, created_at       -- 생성 결과 캐시
npcs           id, room_id, persona_seed, sensitive_flags[]
npc_lines      npc_id, state_hash, text
players        id, name, x, y, hp
player_items   player_id, item_id, qty                     -- 소지품. 0개는 행이 없다
```

소지품이 `players.inventory` 블롭이 아니라 별도 표인 이유: 수량 갱신이 한 문장으로
원자적이어야 한다. 블롭이면 전부 읽고-고쳐-쓰기라, 두 명이 같은 적을 동시에 잡거나
한 사람이 두 탭에서 물약을 마시면 그 사이에 낀 갱신이 사라진다.
(`players.seen` 이 JSON 인 것은 괜찮다 — 통째로만 읽고 쓰며 경합이 없다.)

`state_hash`는 **그 방이 선언한 플래그들의 값만** 해시한 것이다.
전체 월드 플래그를 해시하면 플래그 하나 바뀔 때마다 모든 방의 캐시가 날아간다.

방마다 반응할 플래그를 반드시 좁게 선언한다. 이걸 안 하면 방 하나가
2^n 개의 상태를 갖게 되고 생성 비용이 폭발한다.

캐시 조회 → 없으면 생성 → DB 기록. 이 순서는 항상 동일하다.
플래그가 되돌아가면 예전 `state_hash`의 텍스트가 그대로 복구된다 (의도된 동작).

### 지역

세계는 지역들로 나뉘고, 지역 하나가 세 가지의 단위를 겸한다.

- **관심영역** — `canSee` 가 지역 동일성이다. 지역 하나가 곧 팬아웃의 상한.
- **서버측 안개** — 스냅샷은 '지금 있는 지역' 의 타일만 싣는다. 다른 지역의
  지도는 클라이언트에 아예 가지 않는다.
- **진행** — 지역 간 문은 플래그로 잠글 수 있다. 지역이 '얻는 것' 이 된다.

그래서 지역은 50방쯤으로 유지한다. 1000방을 한 지역에 넣으면 반경 기반
`canSee`, 창(window) 미니맵, 스냅샷 분할이 한꺼번에 필요해진다.

지역 간 문은 **벽 자리에만** 둔다. 걸어갈 수 있는 칸을 가리키면 같은 키
입력에 두 가지 뜻이 생긴다 — 한 칸 이동인가 지역 이동인가.
와이어에서 벽과 잠긴 문은 구별되지 않는다 (둘 다 `ack{blocked}`). 구별할 수
있으면 사방으로 밀어 보는 것만으로 숨긴 문을 전부 찾아낼 수 있다.

---

## 이벤트 → 재렌더링

1. 엔진이 플래그를 켠다 (예: `guardian_slain = true`)
2. 미리 써둔 문장을 즉시 브로드캐스트한다
3. 그 플래그를 `sensitive_flags`에 선언한 방·NPC만 큐에 넣는다
4. 워커가 하나씩 재생성해 DB에 기록한다
5. 새 텍스트는 **다음 입장부터** 적용한다

지금 그 방에 서 있는 플레이어의 화면을 갈아치우지 않는다.
대신 "멀리서 무언가 무너지는 소리가 들린다" 같은 이벤트 메시지만 보낸다.

---

## 입력 처리

키보드(화살표/Enter/Esc), 화면 버튼, 자유 텍스트 입력은 모두
서버로 가기 전에 동일한 액션 객체로 수렴한다.

```ts
{ type: "move", dir: "north" }
{ type: "attack", targetId: "..." }
{ type: "use_item", itemId: "..." }
```

새 입력 방식을 추가할 때는 이 객체를 만드는 어댑터만 추가한다.
액션 처리 로직을 입력 방식별로 분기하지 않는다.

서버는 클라이언트가 보낸 액션의 유효성을 전부 재검증한다.
클라이언트가 보낸 좌표·데미지 값은 신뢰하지 않는다.

---

## 스택

- 클라이언트: React + Vite + TypeScript
- 서버: Node + TypeScript, `ws` (WebSocket)
- DB: SQLite (`better-sqlite3`). 나중에 Postgres로 옮길 수 있게 쿼리를 한 곳에 모을 것
- LLM: `@anthropic-ai/sdk`, 서버에서만. 키는 `.env`

---

## 디렉터리

```
content/
  balance/     적·스킬·아이템·플레이어 수치 (JSON). 사람이 고치는 곳
server/
  engine/      맵(지역·씨앗·출구), 전투, 이동 — LLM도 DB도 파일도 import 하지 않는다
  content/     content/balance/ 를 읽어 검증한다. engine 의 계약으로 바꿔서 넘긴다
  narration/   LLM 호출, 프롬프트, 큐
  db/          스키마, 쿼리, 마이그레이션
  net/         WebSocket 핸들러, 정적 파일
  world/       engine + db + narration + net 을 조합하는 곳
  tools/       선생성 같은 운영 도구
client/
  ui/          미니맵, 로그, 커맨드 윈도우
  input/       키보드/터치/자유텍스트/메뉴 → 액션 객체
shared/        액션 타입, 메시지 타입
```

`engine/`이 `narration/`을 import 하는 코드가 생기면 규칙 1이 깨진 것이다.

### 무엇이 코드고 무엇이 데이터인가

수치는 바꿔도 공짜다 — 되돌릴 수 있고 LLM 재생성을 부르지 않는다. 그래서
`content/balance/` 의 JSON 이고, 고치고 서버만 다시 띄우면 된다.

씨앗·맵 구조·`sensitive_flags` 는 반대다. 씨앗을 고치면 `seed_id` 가 바뀌어
**재생성 비용이 발생하고**, `sensitive_flags` 를 하나 늘리면 그 방의 상태 수가
2배가 된다. 그건 리뷰를 거쳐야 하므로 코드에 남는다.

규칙(치명타 계산, 어그로, 큐 한 자리)은 데이터로 표현할 수 없다. 표현하려
들면 스크립트 언어가 된다.

`engine/` 은 파일을 읽지 않는다. `server/content/` 가 읽어 검증하고, `index.ts`
가 주입한다 — 난수·시계·렌더러와 똑같은 방식이다.

---

## 로드맵

**1단계 — LLM 없이 멀티플레이어**
하드코딩된 방 설명으로 서버 권위 이동, WebSocket 동기화, 두 브라우저에서
서로 보이는 것까지. 여기가 제일 어렵다. LLM은 아직 건드리지 않는다.

**2단계 — 생성 파이프라인**
씨앗 → LLM → DB 고정. 좌표 락, `state_hash` 캐시, 실패 시 폴백 텍스트.

**3단계 — 이벤트 재렌더링**
플래그, 영향 범위, 백그라운드 워커.

**4단계 — NPC와 전투**
NPC 대사도 같은 (씨앗 + 플래그) 패턴으로. 전투는 서버 판정.

**5단계 — UI**
JRPG 커맨드 윈도우, 미니맵, 모바일 터치 조작.

---

## 작업 방식

- 한 번에 한 단계씩. 단계를 건너뛰지 않는다.
- 스키마를 바꿔야 할 것 같으면 코드를 고치기 전에 먼저 제안하고 확인받는다.
- LLM 호출을 새로 추가할 때는 그 호출이 캐시를 거치는지 먼저 확인한다.
- 프롬프트는 `narration/prompts/`에 파일로 분리한다. 코드에 인라인하지 않는다.
