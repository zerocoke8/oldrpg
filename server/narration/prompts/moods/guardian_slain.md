<!-- world_flags.guardian_slain 이 켜졌을 때의 톤.
     `# prompt` 는 LLM 에게 주는 지시, `# fallback` 은 LLM 없이(또는 실패 시)
     문장 뒤에 붙는 결정론적 한 문장이다. 둘 다 '문장' 이므로 코드가 아니라
     여기에 산다. -->

# prompt

이 구역을 지키던 그림자 파수꾼은 방금 쓰러졌다.
위협이 사라진 직후의 느슨한 정적을 담아라.
공기가 가벼워지고, 어둠이 덜 적대적으로 느껴진다.

# fallback

위협이 사라진 뒤의 느슨한 정적이 감돈다.
